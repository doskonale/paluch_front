import { DateTimeTrimmerPipe } from './date-time-trimmer.pipe';

describe('DateTimeTrimmerPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeTrimmerPipe();
    expect(pipe).toBeTruthy();
  });
});
